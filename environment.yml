name: phys170-2023

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10 # pinned by libfmp-1.2.3
  - ipykernel
  - shiny
  - matplotlib
  - numpy
  - scipy
  - jupyter
  - pip
  - pip:
    - shinylive
    - libfmp      # sound library
